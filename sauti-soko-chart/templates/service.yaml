apiVersion: v1
kind: Service
metadata:
  name: {{ include "sauti-soko.fullname" . }}
  labels:
    {{- include "sauti-soko.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sauti-soko.selectorLabels" . | nindent 4 }}
